# Exercício 0.6 - Nova nota na aplicação SPA

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: Usuário digita a nova nota e clica em "Save"

    browser->>server: POST /new_note_spa
    activate server
    server-->>browser: Resposta HTTP 200 OK
    deactivate server

    Note right of browser: JS adiciona a nova nota ao DOM localmente, sem recarregar ou pedir novamente ao servidor
